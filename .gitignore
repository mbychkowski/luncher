# Local Environment Configuration
.env
.env.local
.env.*.local

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
*.tfvars.json
*out.tfplan
.terraform.lock.hcl

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.venv/
venv/
ENV/
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# OS Files
.DS_Store
Thumbs.db

# ADK (Agent Development Kit) Local Storage & Deployment Metadata
.adk/
.adk
deployment_metadata.json
booked_meetings.json

# Scratch space for session handoffs and working notes. Never committed: it
# accumulates cross-repo paths and local context that do not belong in history.
.scratch/

# Eval run outputs: traces and grade results are per-run artifacts.
agents/*/artifacts/
