[project]
name = "strat-agent"
version = "0.1.0"
description = "Corporate Strategy Synthesizer Agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk[a2a]>=2.0.0",
    "uvicorn",
    "pypdf",
    "google-cloud-storage",
    "python-dotenv",
    "starlette",
    "sse-starlette",
]

[tool.uv]
package = false
